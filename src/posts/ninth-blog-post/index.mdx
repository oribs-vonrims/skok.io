---
title: Ninth Blog Post
date: 2017-01-08
published: true
---

# h1 heading

My ninth post `yarn add`

```jsx {4} lineNumbers=false
// jsx {4} lineNumbers=false
<>
  <h1>React Live</h1>
  <h1>React Live</h1>
  <h1>React Live</h1>
</>
```

```jsx {1-3} live lineNumbers=true
// jsx {1-3} live noInline lineNumbers=true
<>
  <h1>React Live</h1>
  <h1>React Live</h1>
  <h1>React Live</h1>
</>
```

```jsx {1,3} lineNumbers=false live
// jsx {1,3} lineNumbers=false live
<>
  <h1>React Live</h1>
  <h1>React Live</h1>
  <h1>React Live</h1>
</>
```

```jsx {1-3} live
// jsx {1-3} live
<>
  <h1>React Live</h1>
  <h1>React Live</h1>
  <h1>React Live</h1>
</>
```

```jsx {2,4}
// jsx {2,3}
<>
  <h1>React Live</h1>
  <h1>React Live</h1>
  <h1>React Live</h1>
</>
```

```jsx live
// jsx live
class Counter extends React.Component {
  constructor() {
    super()
    this.state = { count: 0 }
  }

  componentDidMount() {
    this.interval = setInterval(() => {
      this.setState(state => ({ count: state.count + 1 }))
    }, 1000)
  }

  componentWillUnmount() {
    clearInterval(this.interval)
  }

  render() {
    return (
      <center>
        <h3>
          {this.state.count}
        </h3>
      </center>
    )
  }
}
```

```jsx live noInline=true
// jsx live noInline=true
const Wrapper = ({ children }) => (
  <div style={{
    background: 'papayawhip',
    width: '100%',
    padding: '2rem'
  }}>
    {children}
  </div>
)

const Title = () => (
  <h3 style={{ color: 'palevioletred' }}>
    Hello World!
  </h3>
)

render(
  <Wrapper>
    <Title />
  </Wrapper>
)
```

```js live
// js live
() => (
  <h3>
    So functional. Much wow!
  </h3>
)
```

```jsx
// jsx
<h1>React Live</h1>
```

```jsx live
// jsx live
<>
  <h3>Hello</h3>
  <h3>World</h3>
  <h3>Hello</h3>
  <h3>World</h3>
  <h3>Hello</h3>
  <h3>World</h3>
  <h3>Hello</h3>
  <h3>World</h3>
</>
```

```jsx {1,3-5} lineNumbers=false
// jsx {1,3-5} lineNumbers=false
<>
  <h3>Hello</h3>
  <h3>World</h3>
  <h3>Hello</h3>
  <h3>World</h3>
  <h3>Hello</h3>
  <h3>World</h3>
  <h3>Hello</h3>
  <h3>World</h3>
</>
```

## h2 heading

```
pre pre pre pre pre pre pre pre pre pre pre pre pre pre pre pre pre pre
pre pre pre pre pre pre pre pre pre pre pre pre pre pre pre pre pre pre
pre pre pre pre pre pre pre pre pre pre pre pre pre pre pre pre pre pre
pre pre pre pre pre pre pre pre pre pre pre pre pre pre pre pre pre pre
pre pre pre pre pre pre pre pre pre pre pre pre pre pre pre pre pre pre
```

### h3 heading

```jsx {1,3-5,7} lineNumbers=true
// jsx {1,3-5,7} lineNumbers=true
import React from 'react';

const Dump = props => (
  <div
    style={{
      fontSize: 20,
      border: '1px solid #efefef',
      padding: 10,
      background: 'white',
    }}>
    <h1>React Live</h1>
    {Object.entries(props).map(([key, val]) => (
      <pre key={key}>
        <strong style={{ color: 'white', background: 'red' }}>
          {key} 💩
        </strong>
        {JSON.stringify(val, '', ' ')}
      </pre>
    ))}
  </div>
);

export default Dump;
```


### h3 heading

```jsx {1,3-5,7} lineNumbers=false
// jsx {1,3-5,7} lineNumbers=false
import React from 'react';

const Dump = props => (
  <div
    style={{
      fontSize: 20,
      border: '1px solid #efefef',
      padding: 10,
      background: 'white',
    }}>
    <h1>React Live</h1>
    {Object.entries(props).map(([key, val]) => (
      <pre key={key}>
        <strong style={{ color: 'white', background: 'red' }}>
          {key} 💩
        </strong>
        {JSON.stringify(val, '', ' ')}
      </pre>
    ))}
  </div>
);

export default Dump;
```