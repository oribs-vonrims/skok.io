const transitions = {
  codeBlockCopyButton: `opacity 200ms ease, background-color 200ms ease`,
  codeBlockLanguageLabelIsFocused: `box-shadow 200ms ease`,
  globalStyles: `box-shadow 200ms ease`,
}

export default transitions
